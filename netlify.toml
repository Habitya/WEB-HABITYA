# ───────────────────────────────────────────────
# Publica desde la raíz y redirige todo a index.html
# ───────────────────────────────────────────────

[build]
  publish = "."
  command = ""

[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
  Añade netlify.toml con configuración de build y redirects

